# Shared experiment helpers

`Experiment.invoke(tool, inputs, context)` is the adapter interface. This stdlib-only package provides structured failures, provenance, atomic JSON writing and file hashing. It contains no physics-specific logic.
