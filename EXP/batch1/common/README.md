# Wing comparison

`MODE=fixed|agent EXP/batch1/common/code/run_comparison.sh` checks that the two wing configurations differ only by workflow mode and run identity, then runs the selected mode. Inputs are in `CONFIGs/batch1/aero2d/comparison/`.
